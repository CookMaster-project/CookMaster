import { CategoryService } from "./category.service"

describe("CategoryService",()=>{
    service: CategoryService
    const mockModel = {
        findall: jest.fn(),
        update: jest.fn(),
        findByPk: jest.fn(),
        
    }
})